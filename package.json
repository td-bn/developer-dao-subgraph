{
  "name": "developer-dao",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/developer-dao",
    "create-local": "graph create --node http://localhost:8020/developer-dao",
    "remove-local": "graph remove --node http://localhost:8020/developer-dao",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 developer-dao"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0",
    "matchstick-as": "^0.1.5"
  }
}
